{
  "name": "student-information-drive-app",
  "version": "2.0.0",
  "private": true,
  "description": "Simple student information app using Express, Google Sheets and personal Google Drive, ready for Vercel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check src/auth.js && node --check src/googleDrive.js",
    "show:refresh-token": "node scripts/show-refresh-token.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "googleapis": "^144.0.0",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
